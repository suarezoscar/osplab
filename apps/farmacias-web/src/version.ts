/** Versión de Farmacias de Guardia (farmacias.osplab.dev). Fuente: @osplab/shared-interfaces */
import { PROJECT_VERSIONS } from '@osplab/shared-interfaces';
export const APP_VERSION = PROJECT_VERSIONS['farmacias-web'];
